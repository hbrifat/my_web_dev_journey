let num1 = ['5', '7', '9', '13'];

let myName = 'Bahalul';

let favouriteNumber = 4;

if(Array.isArray(num1)){
    console.log("num1 is a array!");
} else{
    console.log("num1 isn't a array!");
}

if(Array.isArray(myName)){
    console.log("myName is a array!");
} else {
    console.log("myName isn't a array!");
}

if(Array.isArray(favouriteNumber)){
    console.log("favouriteNumber is a array!"); 
} else {
    console.log("favouriteNumber isn't a array!"); 
}